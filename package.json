{
  "name": "genlayer-ai-content-verifier",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "genlayer-js": "latest"
  }
}
